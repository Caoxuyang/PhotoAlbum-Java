# Copy this file to .env and fill in the values before running the application.
# The .env file is excluded from version control (see .gitignore).

# Database password for the Oracle database and the photoalbum application user.
# Used by docker-compose.yml and the Spring Boot application (SPRING_DATASOURCE_PASSWORD).
DB_PASSWORD=YOUR_SECURE_PASSWORD_HERE
