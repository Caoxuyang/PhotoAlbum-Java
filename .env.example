# Copy this file to .env and set values before running with Docker Compose.
# The .env file is excluded from version control (see .gitignore).

# Database password for the Oracle database and application datasource
DB_PASSWORD=CHANGE_ME
