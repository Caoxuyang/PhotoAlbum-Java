# Database credentials - copy this file to .env and fill in your values
# .env is excluded from version control (see .gitignore)

# Oracle database username
DB_USERNAME=photoalbum

# Oracle database password (required - no default)
DB_PASSWORD=

# Optional: override the datasource URL
# DB_URL=jdbc:oracle:thin:@oracle-db:1521/FREEPDB1
