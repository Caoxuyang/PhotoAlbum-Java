# Copy this file to .env and fill in real values before running the application.
# The .env file is excluded from version control (see .gitignore).

# Oracle database admin password (used to initialise the Oracle container)
ORACLE_PASSWORD=

# Application database credentials
DB_USERNAME=
DB_PASSWORD=
